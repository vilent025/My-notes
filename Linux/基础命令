1.ls
-a ：展示所有文件
-l ：以列表的形式展示内容，并展示更多信息
-h ：以易于阅读的形式，列出文件的大小

语法中的选项可以组合使用，比如：
ls -l -a
ls -al
ls -la
是一样的


2.cd
语法：cd 路径
更改当前所在的工作目录
cd 无需选项，只有参数，表示切换到哪个目录下
cd命令直接执行，不写参数，表示回到用户的HOME目录

3.pwd
直接输入
查看当前的工作目录

4.特殊路径符： .  ..  ~
. : 表示当前目录
.. : 表示上一级目录
~ ：表示HOME目录

5.mkdir
创建新的目录
参数必填
-p：表示自动创建不存在的父目录， 适用于创建连续多层级的目录

6.touch
表示要创建的“文件”路径，相对、绝对、特殊路径均可以使用
无选项、参数必填

7.cat
查看文件内容
没有选项，参数必填

8.more
查看文件内容，与cat不同，more支持翻页，如果内容过多，可以一页一页展示
没有选项，参数必填
按q即可退出

9.cp
用于复制文件和文件夹
cp 参数一 参数二
参数一：表示被复制的文件或文件夹
参数二：表示要复制去的地方
-r ：用于复制文件夹

10.mv
移动文件或文件夹
mv 参数一 参数二
参数一表示被移动的文件或文件夹
参数二表示要移动去的地方，如果目标不存在则参数一进行改名，确保目标存在

11.rm
删除文件或文件夹
rm 参数一 参数二 ....... 参数N（参数表示要删除的文件夹或文件）
-r ：用于删除文件夹
-f ：强制删除
rm命令支持通配符*，用来做模糊匹配
test* ：表示匹配任何以test开头的内容
*test ：表示匹配任何以test结尾的内容
*test* ：表示匹配任何包含test的内容
 
12.which
查看命令的程序文件存放在哪里

13.find
搜索指定的文件
find 起始路径 -name “被查找文件名”
-name ：以文件名搜索
-size ：以大小搜索
find -size +/-n[KMG]
	+/-表示大于和小于
	n表示大小数字
	KMG表示大小单位，k（小写）：kb， M：MB，G：GB
支持通配符*
test* ：表示匹配任何以test开头的内容
*test ：表示匹配任何以test结尾的内容
*test* ：表示匹配任何包含test的内容
 
14.grep
从文件中通过关键字过滤文件行
grep 关键字 文件路径
-n ：可选，表示在结果中显示匹配的行的行号
可做内容的输入端口

15.wc
统计文件的行数、单词数量等
wc 文件路径
-c ：统计bytes数量
-m ：统计字符数量
-l ：统计行数
-w ：统计单词数量

16.管道符：|
含义是将管道符左边命令的结果，作为右边命令的输入

17.echo
在命令行内输出指定内容
echo 输出的内容

18.反引号`
echo `  `
被` `包围的内容会被作为命令执行

19.重定向符：>, >>
>：将左侧命令的结果，覆盖写入到符号右侧指定的文件中
>>：将左侧命令的结果，追加到符号右侧指定的文件中

20.tail
查看文件尾部内容，跟踪文件的最新更改
tail Linux路径
-f：表示持续跟踪
-num：表示查看尾部多少行，不填默认10行

21.su
切换账户
su -  [用户名]
切换后，可以通过exit命令回退到上一个用户

22.sudo
为普通的命令授权，临时以root身份执行
sudo 其它命令
但并不是所有的用户，都有权利使用sudo，需要为普通用户配置sudo认证


23.用户，用户组
命令要在root用户执行
一.用户组管理：
1.创建用户组：groupadd 用户组名
2.删除用户组：groupdel 用户组名
二.用户管理
1.创建用户：useradd 用户名
	-g：-g指定用户的组，不指定-g，会创建同名组并自动加入，指定-g需要组已经存在，
		如已存在同名组，必须使用-g
	-d：-d指定用户HOME路径，不指定，HOME目录默认在：/home/用户名
2.删除用户： userdel 用户名
	-r：删除用户的HOME目录，不使用-r，删除用户时，HOME目录保留
3.id
查看用户所属的组
id 用户名
4.usermod:
将指定用户加入指定用户组
usermod -aG 用户组 用户名

24.getent
查看当前系统中有哪些用户或用户组
查看用户：getent passwd
查看用户组：getent group

25.权限控制信息
r：可查看文件内容
w：可修改此文件
x：可将文件作为程序执行

26.chmod
修改文件、文件夹的权限信息
chomd 权限 文件或文件夹
	示例：chomd u=rwx，g=rx，o=x hello.txt
		   u表示user所属用户权限，g表示group组权限，o表示other其他用户权限
-R：对文件夹内的全部内容应用同样的操作
权限也可以用3位数字来表示：
0无任何权限，
即---
1仅有x权限，
即--x
2仅有w权限
即-w-
3:有w和x权限
即-wx
4:仅有r权限
即r--
5:有r和x权限
即r-x 
6:有r和w权限
即rw-
7:有全部权限
即rwx
所以chmod u=rwx，g=rx，o=x test.txt 可以表示成chmod 751 test.txt

27.chown
修改文件、文件夹的所属用户和用户组
普通用户无法修改，此命令只适用于root用户执行p
chown [用户][:][用户组] 文件或文件夹
-R：对文件夹内的全部内容应用相同规则


28.yum
RPM包软件管理器，用于自动化安装配置Linux软件，并可以自动解决依赖问题
yum [-y] [install | remove | search] 软件名称
-y：自动安装，无需手动确认安装或卸载过程
install：安装  |  remove：卸载  |  search：搜索
需要root权限

29.systemcrl
管理软件（启动、停止、开机自启）（能被system管理的软件也叫服务）
systemcrl start | stop | status | enable | disable 服务名
	start：开启		stop：关闭
	status：查看状态	enable：开启开机自启
	disable：关闭开机自启

30.ln
创建软链接（类似windows中的快捷方式）
ln 被链接的文件或者文件夹  要链接去的目的地
-s：创建软链接

31.date
通过date命令可以在命令行中查看系统的时间
语法：date [-d] [+格式化字符串] 
-d: 按照给定的字符串显示日期，一般用于日期计算
格式化字符串:通过特定的字符串标记，来控制显示的日期格式
	%Y 年
	%y 年份后两位数字（00.99) 
	%M 月份(01.12) 
	%d 日(0131) 	
	%H 小时（00.23）
	%M 分钟（00.59) 
	%S 秒(00.60) 
	%s 自 1970-01-0100:00:00 UTC 到现在的秒数

32.ping
检查指定的网络服务器是否是可联通状态
ping  [-c  num]  ip或者主机名
-c：检查的次数，不适应-c选项的话，将无限次持续检查

33.wget
可以在命令行内下载网络文件
wget  url（下载链接）
-b：可选，后台下载，会将日志写入当前工作目录的wget-log文件

34.curl
可以发送http网络请求，可用于：下载文件、获取信息等
curl  url（要发起请求的网络地址）
-O：用于下载文件，当要发起请求的网络地址是下载链接时，可以使用此选项保存文件

35.nmap（需要下载nmap）
查看ip的端口占用情况

36.netstat（需要下载net-tools）
查看指定端口的占用情况

37.ps
查看Linux系统的全部进程
-e：显示出全部的进程
-f：以完全格式化的形式展示信息（展示全部信息） 

38.kill
结束进程
kill 进程IP
-9：强制关闭进程

39.top
查看CPU、内存的使用情况，类似Windows的任务管理器
-p ：只显示某个进程的信息
-d ：设置刷新时间，默认是5s 
	 显示产生进程的完整命令，默认是进程名
-n ：指定刷新次数，比如 top -n 3，刷新输出3次后退出
-b ：以非交互非全屏模式运行，以批次的方式执行top， 一般配合-n指定输出几次统计信息，将输出重定	到指定文件，比如top -b -n 3 >/tmp/top.tmp
-i ：不显示任何闲置(idle)或无用(zombie）的进程
-u ：查找特定用户启动的进程

40.df
查看硬盘的使用情况
-h：以更加人性化的单位显示

41.iostat
查看CPU、磁盘的相关信息
-x：显示更多信息
iostat  num1  num2 ：num1表示刷新间隔  num2表示刷新几次

42.sar
sar -m DEV num1 num2
	-n：查看网络，DEV表示查看网络接口
	num1：刷新间隔，num2：查看次数
（sar 命令很复杂，这里仅简单用于表示统计网络） 


43.tar
进行压缩和解压缩
	常见的两种压缩模式：1.  .tar:称之为tarball，归档文件，即简单地将文件组装到一个.tar的文件内，
						 并没有太多文件体积的减少，仅仅是简单的封装
					    2.  .gz:常见为.tar, .gz, .gzip 格式压缩文件，即使用gzip压缩算法将文件压缩
						 到一个文件内，可以极大地减少压缩后的体积
tar  参数1  参数2  参数N
-c：创建压缩文件，用于压缩模式
-v：显示压缩，解压过程，用于查看进度
-x：解压模式
-f：要创建的文件，或要解压的文件，-f选项必须在所有选项中位置处于最后一个，因为要创建的文件要写       	在后面 
-z：gzip模式，不使用-z就是tarball模式
-C：选择解压的目的地，用于解压模式


44.zip
压缩文件为zip压缩包
-r：被压缩的包含文件夹的时候使用

45.unzip
解压zip压缩包
-d：指定要解压去的文件

 
