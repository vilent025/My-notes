"""
字符串与数组和元组类似：
可以用索引取值，和拥有相同的index方法
同元组一样不支持修改

字符串的替换：
    str.replace(str1, str2)  #将str内的str1替换位str2

字符串的分割：
    str.split(分隔字符串)
    按照指定的分隔字符串，将字符串划分为多个字符串，并存入列表对象中
    
字符串的规整：
    str.strip()
        去除str中前后的空格和换行符
    str.strip(字符)
        去除str中的对应字符，获得一个列表

统计字符串中某字符串出现的次数
    str.count(某字符串)

统计字符串的长度
    len(str)
"""
