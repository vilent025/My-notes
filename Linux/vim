vim/vi 是Linux的文本编辑器，vim是vi的加强版本
vim的三种工作模式：
1.命令模式：
	这个模式下，所敲的按键编译器都理解为命令，以命令驱动执行不同的功能。
	此模型下，不能自由进行文本编辑
2.输入模式：
	也就是编辑模式、插入模式。
	此模式下，可以对文本内容进行自由编辑
3.底层命令模式：
	以：开始，通常用于文件的保存和退出

-------------------------------------------------------------------------------------------------
命令模式快捷键：
i：在当前光标位置进入输入模式

esc：任何情况下摁esc都能回到命令模式

键盘上、k键：向上移动光标
        下、j键：向下移动光标
	左、h键：向左移动光标
	右、l键：向右移动光标

0：移动光标到当前行的开头

$：移动光标到当前行的结尾

pageup：向上翻页
pagedown：向下翻页

/：进入搜索模式
n：向上继续搜索
N：向下继续搜索

dd：删除光标所在行的内容
ndd：删除当前光标向下n行

yy：复制当前行
nyy：复制当前行和下面的n行
p：粘贴复制的内容

u：撤销修改

ctrl + r：反向撤销修改

gg：跳到首行

G：跳到行尾

dG：当前行开始，向下全部删除
dgg：当前行开始，向上全部删除

d0：从当前光标开始，删除本行的结尾
d$：从当前光标开始，删除到本行的开头
---------------------------------------------------------------------------------------------

底线命令模式：
:wq ：保存并退出
:q ：仅退出
:q! ：强制退出
:w ：仅保存
:set nu ：显示行号
:set paste ：设置粘贴模式
