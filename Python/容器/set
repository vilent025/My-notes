"""
集合：
具有互异性，无序性

1.字变量
{元素1，元素2,...}
2.定义变量
变量名称 = {元素1，元素2，...}
3.定义空列表
变量名称 = {}
变量名称 = list{}

"""
"""
操作：
增加新元素：
    set.add("ele")
    
移除元素：
    set.remove("ele")
    
随机取出一个元素
    set.pop("ele")

清空集合
    set.clear()
    
取两个集合的差集
    set3 = set1.difference(set2)  #Set3 = Set1 - Set2 （集合1有而集合2没有的）（集合1和集合2不变）
    
消除两个集合的差集
    set1.difference_update(set2)  #在Set1内，删除和Set2相同的元素 （集合1被修改）
    
合并集合
    set3 = set1.union(set2)  #Set3 = Set1 + Set2  (集合1和集合2不变)
    
统计集合元素数量
    num = len(set)
    
遍历集合
    不能用while
    for ele in set:
        print(ele)

"""
